.main
  .target
    .target-new
      目標を設定
      =link_to new_target_path do
        .target-new__title
          Target Set
    = form_with(url: search_targets_path, local: true, method: :get, class: "search-form") do |form|
      .search-form__title
        目標を共有、Pareを作成
      = form.text_field :keyword, placeholder: "目標を検索する", class: "search-form__input"
      = form.submit "Search", class: "search-form__button"
    .target-list
      .target-list__title
        目標リスト
        .target-mylist__title
          自分の目標(新着順)
        - @targets.each do |target|
          .target-mylist__box
            .target-mylist__box-name
              = link_to target_path(target.id) do
                = target.target
            .target-mylist__box-explanation
              = target.explanation
        .target-mylist__plus
          = link_to myindex_targets_path(current_user.id) do
            more
