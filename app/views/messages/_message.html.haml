.message
  .upper-message
    .upper-message__user
      = message.user.nickname
    .upper-message__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .lower-message
    .lower-message__content
      = message.message